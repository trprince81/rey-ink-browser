const SB=process.env.SUPABASE_URL;const KEY=process.env.SUPABASE_SERVICE_ROLE_KEY;const out=(res,x,s=200)=>{res.statusCode=s;res.setHeader('content-type','application/json; charset=utf-8');res.setHeader('cache-control','public, max-age=60');res.setHeader('access-control-allow-origin','*');res.end(JSON.stringify(x))};export default async function handler(req,res){if(req.method!=='GET')return out(res,{ok:false,error:'Método no permitido'},405);if(!SB||!KEY)return out(res,{ok:false,error:'Servicio no configurado'},503);try{const r=await fetch(SB+'/rest/v1/rey_ink_plans?status=eq.active&duration_days=eq.30&select=id,name,duration_days,max_profiles,max_devices,max_sessions,status&order=max_profiles.asc',{headers:{apikey:KEY,authorization:'Bearer '+KEY}});if(!r.ok)return out(res,{ok:false,error:'No se pudieron cargar los planes'},500);const plans=(await r.json()).filter(p=>[5,10,35,50,100,500,1000].includes(Number(p.max_profiles)));return out(res,{ok:true,program:'Mutant',plans})}catch(_){return out(res,{ok:false,error:'Error al cargar planes'},500)}}