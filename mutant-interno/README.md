# MUTANT — Generador Interno

Aplicación interna para generar licencias MUTANT.

Flujo: correo + plan → generar licencia → registrar en Supabase → mostrar/copi ar licencia.

Backend utilizado: Supabase Edge Function `mutant-license-api`.

Archivo principal: `index.html`.
